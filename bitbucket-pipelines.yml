image: homeassistant/home-assistant

pipelines:
  pull-requests:
    develop/*:
      - step:
          size: 1x
          script:
            - echo 'test pipeline!'
          trigger: automatic
          name: Test
